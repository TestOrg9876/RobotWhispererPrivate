# Record a live topic, then play the recording back as its own connection.

wait 900
click 105 888
wait 900
click 1403 81
wait 700
click 1384 158
wait 500
click 1269 211
type Simulator
wait 500
click 1395 242
wait 1500
click 1327 16
wait 600
click 1272 391
wait 900

# Watch the counter.
click 244 49
wait 1400
click 700 100
wait 700
type /dummy/counter
wait 700
key Return
wait 600
click 1364 96
wait 1500

# Start recording from the footer, let some messages land, stop.
click 1408 888
wait 3000
shot 01-recording
click 1408 888
wait 900
shot 02-recorded

# Replay it: the recording becomes a connection of its own, and a request
# pointed at it streams the captured messages back.
key ctrl+k
wait 700
shot 03-palette
# Clicked rather than typed and confirmed: without a window manager the
# palette's field never takes keystrokes (see xtask/src/native.rs).
#
# "Replay last recording", the ninth row. The palette's list is virtualised
# now, so its rows are a contiguous 36px apart rather than 36 with a gap, and
# every row below the first has moved.
click 595 476
wait 2500
shot 03-replay-connection

# A new request against the replayed system. The system is chosen first and
# the topic second, which is the order a person works in — and it keeps the
# target field's suggestion list from covering the selector.
click 244 49
wait 1400
click 1233 104
wait 700
shot 04-pick-connection
click 1277 161
wait 900
click 700 100
wait 700
type /dummy/counter
wait 700
key Return
wait 600
click 1364 96
wait 2500
shot 05-replaying
