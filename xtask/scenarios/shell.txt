# The default shell: welcome screen, an environment, and one open request.
#
# Coordinates are relative to the app window, which GPUI opens at 1440x900 —
# `screenshot-native` prints the size it found, so a mismatch is visible rather
# than silently clicking empty space. A deliberate layout change is expected to
# move these.

wait 800
shot 01-welcome

# "Add environment" on the welcome hero creates the Dummy connection.
click 926 529
wait 1000
shot 02-environment

# "New request" opens the request editor with an empty topic request.
click 775 529
wait 1200
shot 03-request

# Point the request at a Dummy topic and subscribe. Frames arrive on a transport
# thread and the panel repaints on a timer, so the wait is deliberate.
click 700 133
type /dummy/wave
wait 400
click 1364 133
wait 3000
shot 04-streaming

# The kind selector switches to a service, which adds the payload card.
click 330 133
wait 600
shot 05-kind-menu
