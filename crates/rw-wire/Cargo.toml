[package]
name = "rw-wire"
version = "0.1.0"
edition.workspace = true
description = "Cross-target clock and per-frame timing trace for the transports"

[lib]
path = "src/lib.rs"

[dependencies]

[target.'cfg(target_family = "wasm")'.dependencies]
web-sys = { version = "0.3", features = ["Window", "Performance"] }
