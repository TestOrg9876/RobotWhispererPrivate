# Folders: creating one, naming it, and moving a request into it.
#
# Coordinates are relative to the app window, which GPUI opens at 1440x900.

wait 900

# Two requests to arrange.
click 261 81
wait 1400
click 261 81
wait 1400
shot 01-two-requests

# A folder, named as it is created.
click 232 81
wait 900
type Arm
wait 400
key Return
wait 900
shot 02-folder

# The request's own menu offers the folders it can move into.
rightclick 139 201
wait 800
shot 03-menu

# Moving it in indents it under the folder, and the folder's count follows.
click 162 303
wait 1200
shot 04-moved

# Collapsing hides the contents but keeps the count.
click 40 125
wait 700
shot 05-collapsed
