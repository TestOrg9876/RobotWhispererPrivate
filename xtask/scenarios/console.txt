# The console, with real events in it.
#
# Coordinates are relative to the app window, which GPUI opens at 1440x900.

# Two connections, so there is something to log.
wait 900
click 789 484
wait 900
click 1403 81
wait 700
click 1384 158
wait 500
click 1269 211
type Simulator
wait 500
click 1395 242
wait 1400
click 1403 81
wait 700
click 1384 158
wait 500
click 1269 211
type Bag replay
wait 500
click 1395 242
wait 1600

# Disconnect one, to log that too.
click 1313 128
wait 1000

# Open the console from the footer's toggle.
click 38 888
wait 900
shot 01-console

# The robot's own log lands here too, off /rosout, on the same route and in
# the same order as the app's own events — "did my request go out before that
# node complained" is a question about one ordering, and two windows cannot
# answer it.
wait 9000
move 700 500
shot 02-rosout

# The level filter is a menu rather than a button that cycles. Three levels
# with the current one checked, so both what else there is and the way back are
# visible — a cycling button spends the same pixel and shows neither.
click 510 747
wait 800
shot 03-level-menu
click 530 803
wait 800
shot 04-warnings

# And back through the menu to errors only, which is the level anybody actually
# reaches for.
click 510 747
wait 800
click 530 831
wait 800
shot 05-console-errors
