# Discovery offers on the target field: the connections tree's replacement.
#
# Coordinates are relative to the app window, which GPUI opens at 1440x900.

# A Dummy ROS system, so the rest of the scenario has something to talk to.
wait 900
click 105 888
wait 900
click 1403 81
wait 700
click 1384 158
wait 500
click 1269 211
type Simulator
wait 500
click 1395 242
wait 1500
click 1327 16
wait 600
click 1272 102
wait 900

# A request, pointed at the new connection by default.
click 244 49
wait 1400

# Clicking the target field shows everything the system advertises.
click 700 92
wait 900
shot 01-all-offers

# Typing narrows it and the keyboard takes the match. `/dummy/w` matches one
# topic, so Enter accepts `/dummy/wave` — which is what the last shot shows.
#
# The narrowed *popup* is deliberately not photographed. It is an overlay, and
# under this harness it does not repaint between a keystroke and a capture, so
# any shot of it would show the unfiltered list under a name claiming
# otherwise. What the ranking actually does is asserted in `discovery.rs`'s
# own tests; what this proves on screen is that the keyboard reaches the field
# and the accepted match lands in it.
click 700 92
wait 600
type /dummy/w
wait 800
key Return
wait 800
move 700 500
wait 400
shot 04-accepted
