# The sidebar with real work in it: three kinds of request, named and targeted,
# arranged into a collection.

wait 900

# A Dummy ROS system, from the footer.
click 105 888
wait 900
click 1403 81
wait 700
click 1384 158
wait 500
click 1269 211
type Simulator
wait 500
click 1395 242
wait 1500
click 1327 16
wait 600
click 1272 102
wait 900

# A collection to put them in.
click 224 49
wait 900
type Dummy robot
wait 400
key Return
wait 900

# A topic, a service and an action — the kind is chosen before the target so
# the offers are the right ones, and the name is replaced rather than typed
# into the middle of the placeholder.
click 244 49
wait 1400
click 700 133
wait 700
type /dummy/wave
wait 700
key Return
wait 600
click 420 89
wait 500
key ctrl+a
wait 300
type Wave data
wait 500
key ctrl+s
wait 1000
click 244 49
wait 1400
click 330 133
wait 700
click 350 189
wait 800
click 700 133
wait 700
type /dummy/add_two_ints
wait 700
key Return
wait 600
click 420 89
wait 500
key ctrl+a
wait 300
type Add two ints
wait 500
key ctrl+s
wait 1000
click 244 49
wait 1400
click 330 133
wait 700
click 349 215
wait 1000
click 700 133
wait 700
type /dummy/fibonacci
wait 700
key Return
wait 600
click 420 89
wait 500
key ctrl+a
wait 300
type Fibonacci
wait 500
key ctrl+s
wait 1000
shot 01-three-kinds

# Drag a request into the collection, which opens as it accepts the drop.
# Rows start at y=107 and are 29px apart: the collection, then the three
# requests at 136, 165 and 194.
drag 139 165 -> 139 107
wait 1300
shot 02-nested

# Subscribing marks the row as live. After the drop the order is the collection,
# the request inside it, then the two still at the root — so Wave data is at 165.
click 139 165
wait 1200
click 1364 133
wait 2000
shot 03-live
