# Freeze and diff: pin a message, let the stream run, and see what moved.

wait 900
click 105 888
wait 900
click 1403 81
wait 700
click 1384 158
wait 500
click 1269 211
type Simulator
wait 500
click 1395 242
wait 1500
click 1327 16
wait 600
click 1272 102
wait 900

# A topic that changes every message, subscribed.
click 244 49
wait 1400
click 700 100
wait 700
type /dummy/counter
wait 700
key Return
wait 600
click 1266 100
wait 2000
shot 01-subscribed

# Pin this message. The stream keeps running underneath.
click 1178 262
wait 300
shot 02-frozen

# Two seconds of counting later: one row, one delta. Every other field in the
# message is unchanged, and an unchanged field is not a row.
wait 2000
move 700 500
shot 03-changed

# Freezing again re-pins to the newest message, so the deltas start from here.
click 1158 262
wait 1000
move 700 500
shot 04-refrozen
