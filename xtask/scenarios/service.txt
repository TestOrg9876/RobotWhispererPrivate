# Calling a service: the schema-driven form, and the response.
#
# Coordinates are relative to the app window, which GPUI opens at 1440x900.

# A Dummy ROS system, so the rest of the scenario has something to talk to.
wait 900
click 105 888
wait 900
click 1403 81
wait 700
click 1384 158
wait 500
click 1269 211
type Simulator
wait 500
click 1395 242
wait 1500
click 1327 16
wait 600
click 1272 102
wait 900

# A request, switched from a topic to a service.
click 244 49
wait 1400
click 331 92
wait 800
click 351 149
wait 800
shot 01-service-kind

# Pick the Dummy service from the offers.
click 700 92
wait 900
shot 02-service-offers
key Return
wait 1000
shot 03-form

# Fill both arguments and call. The inputs start at x=535; clicking at 700
# lands in the label column, which is a quiet way to fill in nothing.
click 975 194
wait 500
type 20
wait 500
click 975 237
wait 500
type 22
wait 500
shot 04-filled
click 1370 92
wait 1800
move 700 500
shot 05-response
