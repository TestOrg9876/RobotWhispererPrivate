# A dashboard: several live views, arranged by hand.

wait 900
click 105 888
wait 900
click 1403 81
wait 700
click 1384 158
wait 500
click 1269 211
type Simulator
wait 500
click 1395 242
wait 1500
click 1327 16
wait 600
click 1272 391
wait 900

# New dashboard, from the sidebar's Dashboards section.
shot 00-sidebar
click 261 835
wait 1800
shot 01-empty-dashboard

# Point the pane at a live topic. The pickers sit in the empty pane itself;
# once it is showing something they are gone and its settings live on the tab
# strip the dock already draws. Topic goes straight to the searchable picker,
# which is the same palette the rest of the app searches with — a flat menu of
# every topic does not survive a robot that publishes three hundred.
click 800 500
wait 900
shot 02-topic-picker
# Chosen by clicking the row. Typing to narrow the list works in the app and
# not under this harness: with no window manager nothing sets input focus on
# the window, which the harness's own notes call out.
click 600 184
wait 1800
shot 02-first-pane

# A second pane, from the dashboard's own header rather than a `+` hiding on
# the dock's tab strip.
click 1382 82
wait 1500
click 1180 500
wait 900
# The rows are 36px apart and contiguous now that the list is virtualised, so
# this is the third of them: /dummy/wave.
click 600 260
wait 1800
shot 03-two-panes

# Dragged by its tab to the bottom edge, which splits the layout there. This is
# the dock's, not ours: a dashboard tiles, and a pane goes where the split tree
# allows rather than to arbitrary coordinates.
dragover 1000 116 -> 800 700
wait 800
release
wait 2000
shot 04-split

# Each pane's settings are on the menu inside its own header: flat, and one
# entry per thing it can be told. The topic is one searchable entry saying how
# many there are rather than all of them, and the message count beside the
# title has the rate with it.
click 830 116
wait 900
shot 05-pane-menu

# Plot, from that menu. Two live panes, two views, one dashboard.
click 741 174
wait 2500
move 700 700
shot 06-dashboard
