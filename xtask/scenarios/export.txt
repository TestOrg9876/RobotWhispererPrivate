# Export from the menu. The file dialog itself is the platform's, and there is
# no portal under a bare X server, so this checks the app survives the attempt
# and says so rather than dying.

wait 900
click 1327 16
wait 700
shot 01-menu
# Export workspace…, in the File section. The coordinate this replaced landed
# five rows short of it, so the shot below was of a scenario that had never
# reached the dialog at all.
click 1272 179
wait 1500
click 38 888
wait 900
shot 02-after-export
