# The world pane: several topics and a robot, each published in its own frame,
# all resolved into one through the transform tree.

wait 900
click 105 888
wait 900
click 1403 81
wait 700
click 1384 158
wait 500
click 1269 211
type Simulator
wait 500
click 1395 242
wait 1500
click 1327 16
wait 600
click 1272 102
wait 900

# Open the 3D world from the flat menu.
click 1327 16
wait 600
click 1231 193
wait 2000
move 700 500
shot 01-empty-world

# The + on the rail's own header row. The topics are behind one searchable
# entry — a flat list of every topic does not survive a robot that publishes
# three hundred — and only the ones with geometry in them are counted: the
# counter and the string are not among the seven.
click 507 90
wait 900
shot 02-add-menu

# The lidar sweep, which arrives in the sensor's own frame.
click 560 117
wait 1000
shot 03-topic-picker
click 600 226
wait 2500
move 700 500
wait 400
shot 04-cloud

# The robot's trail, which arrives in the map. Two topics, two frames, one
# scene — this is the picture that was not expressible before.
click 507 90
wait 900
click 560 117
wait 1000
click 600 378
wait 2500
move 700 500
wait 400
shot 05-cloud-and-path

# A robot from the catalog. Its description's root link is not a frame this
# system publishes, so the layer is dimmed and says exactly that rather than
# being drawn at the origin where it would look plausible and be wrong.
click 507 90
wait 900
# UR10e, the last entry. The list moved down when the connection's own
# published robot was added above it — see the collision note in the knowledge
# base: a coordinate below anything that can grow is a coordinate that will be
# wrong again.
click 530 357
wait 6000
move 700 500
wait 400
shot 06-robot-unplaced

# The one per-layer control that has to exist, on the flat menu the dock
# already draws: which frame the description hangs off.
click 1404 49
wait 900
shot 07-pane-menu
click 1322 75
wait 3000
move 700 500
wait 400
shot 08-robot-on-base-link

# Close enough to see the arm standing where the transform tree put it, on the
# trail it has just driven.
scroll -5
wait 1200
shot 09-robot-close

# The proof. The fixed frame is the one control the whole pane turns on: in
# `map` the room stands still and the robot drives through it; in `base_link`
# the robot stands still and the room swings round it. Neither picture is
# expressible without a transform tree, and before one both of these sat on
# top of each other at the origin.
click 433 90
wait 900
shot 10-frame-menu
click 471 117
wait 3000
move 700 500
wait 400
shot 11-fixed-base-link
