# Sending an action goal, and watching feedback arrive before the result.
#
# Coordinates are relative to the app window, which GPUI opens at 1440x900.

# A Dummy ROS system, so the rest of the scenario has something to talk to.
wait 900
click 789 484
wait 900
click 976 157
wait 800
click 711 246
wait 500
click 720 299
type Simulator
wait 500
click 968 330
wait 1500
# Dismissed by clicking its overlay, well away from the dialog. Aiming at the
# close button risks a near miss, which the overlay then absorbs — and the click
# after that is the one that appears to do nothing.
click 200 750
wait 900

# A request, switched to an action.
# The list's + button, which is there whether or not the list is empty.
click 261 81
wait 1400
click 330 133
wait 600
click 350 217
wait 800

# Pick the Dummy action from the offers.
click 700 133
wait 800
key Return
wait 1000
shot 01-goal-form

# An order to compute, then send.
click 975 251
type 6
wait 300
click 1364 133
wait 1200
shot 02-feedback
wait 4000
shot 03-result
