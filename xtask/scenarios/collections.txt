# The sidebar: collections, kinds, nesting and drag-and-drop.
#
# Coordinates are relative to the app window, which GPUI opens at 1440x900.
# The panel's two actions live on the dock's tab strip: a collection at x=224,
# a request at x=244, both at y=49. Collection rows are 30px tall and request
# rows 42px, starting at y=78.

wait 900

# A collection, named as it is created.
click 224 49
wait 900
type Arm
wait 400
key Return
wait 900

# One nested inside it, from its own menu.
rightclick 139 93
wait 700
click 190 115
wait 900
type Wrist
wait 400
key Return
wait 1000
shot 01-nested

# Two requests at the root.
click 244 49
wait 1400
click 244 49
wait 1400
shot 02-requests

# Drag the last one into the *nested* collection, which is the case worth
# photographing: a request two levels deep is what the indent guides are for.
#
# Rows are 33px apart from y=107, and both requests start at the root.
drag 139 206 -> 139 140
wait 1300
shot 03-dragged

# A collection out of its parent. The empty space below the tree is the root's
# own drop target, so dragging something out is the same gesture as dragging it
# in — and the request nested inside Wrist travels with it.
drag 139 140 -> 139 420
wait 1300
shot 04-collection-out

# And back in. Nothing here stops a collection holding collections; what is
# guarded is dropping one inside itself, and a row that would refuse the drop
# does not light up.
drag 139 140 -> 139 107
wait 1300
shot 05-collection-in
