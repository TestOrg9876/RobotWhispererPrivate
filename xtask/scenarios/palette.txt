# The command palette and settings, and two ROS systems connected at once.
#
# Coordinates are relative to the app window, which GPUI opens at 1440x900.

wait 900

# Two Dummy systems, to show they are concurrent rather than exclusive.
click 789 484
wait 900
click 1403 81
wait 700
click 1384 158
wait 500
click 1269 211
type Simulator
wait 500
click 1395 242
wait 1400

click 1403 81
wait 700
click 1384 158
wait 500
click 1269 211
type Bag replay
wait 500
click 1395 242
wait 1600
shot 01-two-connections

# The palette finds commands, requests and connections in one list. Opened from
# the menu here: the keyboard route needs a focused element, and nothing is
# focused under a bare X server with no window manager.
click 1327 16
wait 700
click 1265 229
wait 900
shot 02-palette

# And it filters. This shot was taken immediately after the one above and named
# "filtered" without anything ever having been typed into the field, so the
# search path — the part that actually runs on every keystroke — was never in a
# picture at all.
click 720 141
wait 500
type conn
wait 800
# GPUI paints on demand and the capture reads whatever the window last
# composited, so a shot that follows typing needs the pointer nudged more than
# once before it: one nudge leaves the previous frame on screen.
move 720 250
wait 400
move 720 300
wait 400
shot 03-palette-filtered

# Settings, chosen from the palette.
click 700 264
wait 900
shot 04-settings

# Choosing a theme applies it immediately — it is a preview, not a form.
click 700 280
wait 900
shot 05-theme-applied
