# A live lidar sweep, orbited by dragging.

wait 900
click 105 888
wait 900
click 1403 81
wait 700
click 1384 158
wait 500
click 1269 211
type Simulator
wait 500
click 1395 242
wait 1500
click 1327 16
wait 600
click 1272 102
wait 900

# The point cloud topic, subscribed.
click 244 49
wait 1400
click 700 100
wait 700
type /dummy/points
wait 700
key Return
wait 600
click 1266 100
wait 2000

# The tree first: a point cloud is a header, a list of fields and sixty
# kilobytes of `data`, and the tree is what makes the first two readable
# without scrolling past the third. `data` arrives folded because it is long.
click 390 155
wait 1200
shot 00-tree

# Folding `fields` puts four sub-trees away behind one row. A folded branch
# costs one row whatever is under it, which is what keeps this cheap on a
# message that arrives ten times a second.
click 345 216
wait 900
shot 00b-folded

# Visualize, which is where the 3D pane lives.
click 460 155
wait 2500
shot 01-cloud

# Drag across the pane to orbit the camera.
drag 700 600 -> 950 520
wait 900
shot 02-orbited

# Colour by intensity instead of height.
click 381 194
wait 900
shot 03-intensity
