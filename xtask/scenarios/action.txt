# Sending an action goal, and watching feedback arrive before the result.
#
# Coordinates are relative to the app window, which GPUI opens at 1440x900.

# A Dummy ROS system, so the rest of the scenario has something to talk to.
wait 900
click 105 888
wait 900
click 1403 81
wait 700
click 1384 158
wait 500
click 1269 211
type Simulator
wait 500
click 1395 242
wait 1500
click 1327 16
wait 600
click 1272 391
wait 900

# A request, switched from a topic to an action.
click 244 49
wait 1400
click 331 92
wait 800
click 348 177
wait 800

# The action the simulator offers, chosen from the autocomplete.
click 700 92
wait 800
type /dummy/fib
wait 800
key Return
wait 1200
shot 01-goal-form

# An order to compute, then send. Feedback arrives while the goal runs, and
# the result replaces it when it finishes.
click 975 194
wait 800
type 8
wait 800
click 1363 96
# Mid-run: the dummy sends a feedback message every 150 ms, so an order of 8
# takes something over a second. Shooting at 700 ms catches the sequence part
# built, which is the whole point of feedback — a shot after it finished would
# be the result under another name.
wait 700
shot 02-feedback
wait 4000
move 700 500
shot 03-result
